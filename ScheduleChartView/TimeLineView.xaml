<UserControl
    x:Class="ScheduleChartView.TimeLineView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  xmlns:local="clr-namespace:ScheduleChartView"
    xmlns:convert="clr-namespace:ScheduleChartView.ViewModels"
    mc:Ignorable="d"  d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- ms -> px 변환 -->
        <convert:TimeToPixelsConverter x:Key="TimeToPx" />
        <!-- 행 인덱스 -> Canvas.Top (Row * RowHeight) -->
        <convert:RowToTopConverter x:Key="RowToTop" />
        <!-- Duration(ms) * PixelsPerMs -->
        <convert:DurationToWidthConverter x:Key="DurToWidth" />

        <!-- Segment UI -->
        <DataTemplate x:Key="SegmentTemplate" DataType="{x:Type local:SegmentVM}">
            <Border CornerRadius="6"
                    Height="{Binding DataContext.SegmentHeight, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                    Background="{Binding Brush}"
                    ToolTip="{Binding Tooltip}">
                <!-- 내용이 필요없으면 비워둠 -->
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="48" />
            <!-- Y축 라벨 -->
            <ColumnDefinition Width="*" />
            <!-- 캔버스 -->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="28" />
            <!-- X축 라벨 -->
        </Grid.RowDefinitions>

        <!-- Y 축 라벨 (0..N-1) -->
        <ItemsControl Grid.Column="0" Grid.Row="0"
                      ItemsSource="{Binding Lanes}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Top">
                        <Setter.Value>
                            <MultiBinding Converter="{StaticResource RowToTop}">
                                <Binding Path="Row" />
                                <Binding Path="DataContext.RowHeight"
                                         RelativeSource="{RelativeSource AncestorType=ItemsControl}" />
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Row}"
                               FontSize="16" VerticalAlignment="Center"
                               Height="{Binding DataContext.RowHeight, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                               HorizontalAlignment="Right" Margin="0,0,6,0" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 타임라인 캔버스 -->
        <ScrollViewer Grid.Column="1" Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Hidden">
            <ItemsControl ItemsSource="{Binding AllSegments}" ItemTemplate="{StaticResource SegmentTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Height="{Binding CanvasHeight}"
                                Width="{Binding CanvasWidth}" Background="#FFFDFDFD" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <!-- 좌표 배치 -->
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left">
                            <Setter.Value>
                                <MultiBinding Converter="{StaticResource TimeToPx}">
                                    <Binding Path="StartMs" />
                                    <Binding Path="DataContext.PixelsPerMs"
                                             RelativeSource="{RelativeSource AncestorType=ItemsControl}" />
                                </MultiBinding>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Canvas.Top">
                            <Setter.Value>
                                <MultiBinding Converter="{StaticResource RowToTop}">
                                    <Binding Path="Row" />
                                    <Binding Path="DataContext.RowHeight"
                                             RelativeSource="{RelativeSource AncestorType=ItemsControl}" />
                                </MultiBinding>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Width">
                            <Setter.Value>
                                <MultiBinding Converter="{StaticResource DurToWidth}">
                                    <Binding Path="DurationMs" />
                                    <Binding Path="DataContext.PixelsPerMs"
                                             RelativeSource="{RelativeSource AncestorType=ItemsControl}" />
                                </MultiBinding>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Height" Value="{Binding DataContext.SegmentHeight, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </ScrollViewer>

        <!-- X 축(ms) 간단 라벨 -->
        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="ms →" Margin="6,0,0,0" FontSize="14" />
            <TextBlock Text="{Binding CanvasWidth, StringFormat=({0:0} px)}" Margin="8,0,0,0" Foreground="#888" />
        </StackPanel>
    </Grid>
</UserControl>